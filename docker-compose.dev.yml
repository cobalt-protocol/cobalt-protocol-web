version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cobalt-protocol-web-dev
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - PORT=3000

  kubo:
    image: ipfs/kubo:latest
    container_name: cobalt_protocol_kubo_dev
    restart: unless-stopped
    ports:
      - "${KUBO_SWARM_PORT:-4001}:4001"
      # Bind API to localhost only on the host machine to allow Web UI access safely
      - "${KUBO_API_PORT:-5001}:5001"
      - "${KUBO_GATEWAY_PORT:-8081}:8080"
    environment:
      IPFS_PROFILE: server
    volumes:
      - kubo_dev_data:/data/ipfs:U
      - kubo_dev_export:/export:U
      - ./kubo-init.sh:/container-init.d/01-bind-api.sh:ro
    networks:
      default:
        aliases:
          - kubo
    healthcheck:
      test: ["CMD", "ipfs", "id"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s

volumes:
  kubo_dev_data:
  kubo_dev_export:


