services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: cobalt-protocol-web
    ports:
      - "3002:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
