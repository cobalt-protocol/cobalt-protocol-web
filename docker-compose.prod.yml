name: cobalt-protocol

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: cobalt-protocol-web
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
      - HOSTNAME=0.0.0.0




